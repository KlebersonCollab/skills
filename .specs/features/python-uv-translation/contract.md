# Contract: Python UV Translation

## 🤝 Delivery Agreement
The `python-uv` skill will be fully localized in English. The translation will be high-fidelity, preserving all technical accuracy, commands, and structural integrity.

## 🔍 Validation Sensors
- **Language Detection**: All markdown files in `python-uv/` must be in English.
- **Link Integrity**: All internal `file:///` links must remain valid.
- **Command Accuracy**: All `uv` commands must be exactly as in the original Portuguese version.
- **Formatting**: Mermaid diagrams and markdown tables must render correctly.

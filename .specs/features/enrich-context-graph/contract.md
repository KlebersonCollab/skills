# Contract: Enrichment of Context Graph

## Agreement
The Implementer will unify the conceptual `context-graph` skill with the technical `memdir` protocol, creating a deterministic standard for agentic memory management.

## Sensors (Validation)
1.  **Limit Presence**: `SKILL.md` must mention the 200-line/25KB limit.
2.  **Format Presence**: `SKILL.md` must include the YAML frontmatter example for topic files.
3.  **Taxonomy**: All 5 memory types (`User`, `Project`, `Local`, `Managed`, `AutoMem`) must be listed.
4.  **Hub Audit**: `hb audit context-graph` must pass.

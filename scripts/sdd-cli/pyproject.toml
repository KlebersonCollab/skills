[project]
name = "sdd-cli"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "questionary>=2.1.1",
    "rich>=15.0.0",
    "typer>=0.24.1",
]

[project.scripts]
sdd = "main:app"
